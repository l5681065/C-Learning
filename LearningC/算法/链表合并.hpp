#pragma once
#include "Node.h"


//将两个递增的有序链表合并为一个递增的有序链表。要求结果链表仍使用原来两个链表的存储空间, 不另外占用其它的存储空间。表中不允许有重复的数据。
template <typename  Data>
Node<Data>* MergeList(Node<Data>* A, Node<Data>* B)
{

	return A;
}


//将两个递增的有序链表合并为一个递增的有序链表。要求结果链表仍使用新的存储空间, 不能改变原来链表的顺序。表中不允许有重复的数据。
template <typename  Data>
Node<Data> * MergeList_NewMemory(Node<Data>* A, Node<Data>* B)
{
	Node<Data> 
	return nullptr;
}

